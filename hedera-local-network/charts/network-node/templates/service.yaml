apiVersion: v1
kind: Service
metadata:
  name: {{ include "network-node.fullname" . }}
  labels:
    {{- include "network-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: TCP
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "network-node.selectorLabels" . | nindent 4 }}
